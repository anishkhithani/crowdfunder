<% provide(:li_class_pj, "class=active") %>
<h1>  <strong> <%= @project.title %></strong> </h1>
<div class="container">
  <div class="projectindex">
    <p>
      <em> Project by user: <%= @project.user_id %> </em><br>
      <%= @project.description %><br>
      Our goal! $<%= @project.goal %><br>
      We've been funding since <%= @project.created_at %>
    </p>
    <button> <%= link_to "Pledge", new_pledge_path %> </button>
  </div>
</div>
